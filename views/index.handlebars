<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <!--<a class="navbar-brand" href="#">AXE</a>-->
      <img src="/images/census-logo-white.png" height="30" style="margin-top: 10px">
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="active"><a href="/">Home</a></li>
        <li><a href="/report/001">Report</a></li>
        <li><a href="/about">About</a></li>
      </ul>
    </div><!--/.nav-collapse -->
  </div>
</nav>

<div class="container">

  <div class="jumbotron">
    <h3>CEDSCI Accessibility Test Suite</h3>
  </div>

  <div class="row">
    <div class="col-lg-12">
      <table class="table table-hover table-striped">
        <thead>
          <tr>
            <th>&nbsp;</th>
            <th>Title</th>
            <th>URL</th>
            <th>Last Run</th>
            <th>Status</th>
          </tr>
        </thead>
        <tbody>
          {{#each tests}}
            <tr>
              <td><a class="btn btn-info" href="/report/{{this.id}}" role="button">View</a></td>
              <td>{{this.title}}</td>
              <td><a href="{{this.url}}">{{this.url}}</a></td>
              <td>{{this.runtime}}</td>
              <td>{{this.violations}} Violations</td>
            </tr>
          {{/each}}
        </tbody>
        <tfoot>
          <tr>
            <th colspan="5">{{tests.length}} Active tests</th>
          </tr>
        </tfoot>
      </table>
    </div>
  </div>

</div>
